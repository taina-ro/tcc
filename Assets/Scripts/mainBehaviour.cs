using UnityEngine;
using System.Collections.Generic;

public class Main : MonoBehaviour
{
    //public string csvFileName = "testeTabela.csv"; // Nome do arquivo CSV a ser lido (coloque o nome do seu arquivo aqui)
    //public string outputCsvFileName = "dados_agrupados.csv"; // Nome do arquivo CSV de saída

    //private CSVReader csvReader = new CSVReader();

    void Start()
    {
        //// Ler o arquivo CSV e gerar o arquivo de saída
        //csvReader.ReadCSV(csvFileName);
       

        //List<RoomData> roomDataList = csvReader.GetRoomDataList();

        //foreach (RoomData roomData in roomDataList)
        //{
        //    float potenciaIluminacao = roomData.PontosDeLuz();
        //    roomData.PotenciaIluminacao = potenciaIluminacao;

        //    // Use potenciaIluminacao as needed in your project
        //   // Debug.Log("Potência de Iluminação para " + roomData.name + ": " + potenciaIluminacao + " VA");
        //}

        //csvReader.GenerateOutputCSV(outputCsvFileName);
    }
   
}
